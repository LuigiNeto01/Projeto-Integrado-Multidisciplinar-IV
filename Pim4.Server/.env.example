# ASP.NET Core API (.NET 8)

# Login de debug (opcional para desenvolvimento)
DEBUG_LOGIN=false
DEBUG_EMAIL=admin@demo.com
DEBUG_PASSWORD=admin

# JWT
JWT_SECRET=dev_super_secret_change_me
JWT_ISSUER=Pim4
JWT_AUDIENCE=Pim4Client

# PostgreSQL (ajuste conforme seu ambiente)
# Exemplo compatível com docker-compose.yml incluso no repo
# services.postgres: user=pim4 password=pim4 db=pim4 port=5432
POSTGRES_CONNECTION=Host=localhost;Port=5432;Username=pim4;Password=pim4;Database=pim4;

# Evitar redirecionamento HTTPS no dev quando usando somente HTTP
DISABLE_HTTPS_REDIRECT=true

# Gemini (opcional) — usado por /ai/chamado/opiniao
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com

